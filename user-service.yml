spring:
  application:
    name: user-service

  security:
    user:
      name: admin
      password: admin123

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka